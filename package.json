{
  "name": "frames-workspace",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/frames && npm run build --workspace=packages/vite-plugin-frames && npm run build --workspace=examples/demo-app && npm run build --workspace=examples/benchmark",
    "test": "npm test --workspace=packages/frames",
    "typecheck": "npm run build --workspace=packages/frames && npm run typecheck --workspace=packages/vite-plugin-frames",
    "check": "npm run test && npm run typecheck && npm run build"
  },
  "devDependencies": {
    "@babel/core": "^8.0.1",
    "@babel/plugin-syntax-jsx": "^8.0.1",
    "@babel/preset-typescript": "^8.0.1",
    "@types/babel__core": "^7.20.5",
    "@types/node": "^26.1.1",
    "jsdom": "^29.1.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
